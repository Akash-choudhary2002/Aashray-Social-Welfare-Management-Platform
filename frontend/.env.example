# Base URL of the API Gateway (Phase 1). All requests go through here.
VITE_API_BASE_URL=http://localhost:8080

# No Google Maps key needed — the Food Donation location picker runs on
# Leaflet + OpenStreetMap tiles, which require no API key at all.
